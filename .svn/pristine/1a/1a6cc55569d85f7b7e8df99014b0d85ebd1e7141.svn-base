/*
 * @(#) ${entity.name}Dao.java $today
 * Copyright $today.substring(8) GPDI. All right reserved.
 */
package ${config.package}.dao;

import org.springframework.stereotype.Repository;
import pub.dao.mybatis.MyBatisDao;
import ${config.package}.dao.${entity.name}Dao;
import ${config.package}.entity.${entity.name};

/**
* @author $config.author
* @version 1.0 $today
*/
@Repository
@SuppressWarnings("unchecked")
public class ${entity.name}Dao extends MyBatisDao<${entity.name}> {

	public ${entity.name}Dao() {
		super(${entity.name}.class);
	}

}
