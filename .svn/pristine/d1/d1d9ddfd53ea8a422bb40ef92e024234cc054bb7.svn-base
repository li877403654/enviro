package test;

import com.google.common.primitives.Ints;
import com.google.common.primitives.Longs;
import org.apache.commons.codec.binary.Hex;
import pub.support.Base64;

/**
 * Created by zzl.
 * Date: 2018-06-25
 */
public class T2 {

    public static void main(String[] args) {

        int id = 9000000;
        byte[] bts = Ints.toByteArray(id);
//        String base64 = Base64.encode(bts);
        String hex = Hex.encodeHexString(bts);
//        String sMobile = String.valueOf(id);
//        assert sMobile.length() == 11;
//        byte[] bts = new byte[5];
//        int btIndex = 0;
//        for(int n = 1; n < 11; n+=2) {
//            byte b = Byte.valueOf(sMobile.substring(n, n + 2));
//           bts[btIndex++] = b;
//        }
//        String hex = Hex.encodeHexString(bts);
        System.out.println("?");

    }

}
